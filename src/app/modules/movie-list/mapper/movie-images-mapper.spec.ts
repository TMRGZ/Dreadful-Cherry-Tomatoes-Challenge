import { MovieImagesMapper } from './movie-images-mapper';

describe('MovieImagesMapper', () => {
  it('should create an instance', () => {
    expect(new MovieImagesMapper()).toBeTruthy();
  });
});
